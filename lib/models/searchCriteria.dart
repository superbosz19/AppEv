class SearchCriteria{

  Map<String, bool> connectors;
  Map<String, bool> locType;
  bool allConnectors;
  bool allLocType ;
  SearchCriteria({this.connectors, this.locType, this.allConnectors:true, this.allLocType:true});


}